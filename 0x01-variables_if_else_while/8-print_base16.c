#include <stdio.h>
/**
 * main - A programme to print numbers of base 16
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;
	char c;

	while (n = 0; n <= 10; n++)
		putchar(n + '0');

	while (c = 'a'; c <= 'f'; c++)
		putchar(c);

	putchar('\n');
	return (0);
}

